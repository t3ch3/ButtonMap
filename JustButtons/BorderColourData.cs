using System;
namespace JustButtons
{
    /// <summary>
    /// Holds the red, green and blue for a colour
    /// Used to hold the colour data so it can be converted to a jSON string
    /// </summary>
    public class BorderColourData
    {
        public float Red = 0;
        public float Green = 0;
        public float Blue = 0;

        public BorderColourData()
        {
        }
    }
}
